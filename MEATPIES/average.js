const prompt = require("prompt-sync")();

let num_one = Number(prompt("Enter the first Number "))
let num_two = Number(prompt("Enter the second Number "))
let num_three = Number(prompt("Enter the second Number "))
let sum = num_one + num_two + num_three;
console.log("The Product of the numbers is " + product)
